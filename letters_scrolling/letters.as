package  {			import flash.display.MovieClip;	import flash.display.Sprite;	import flash.geom.Rectangle;	import flash.events.*;		public class letters extends MovieClip {		var circle:Sprite = new Sprite();		/*var numLetters:Number = 26;		var asciiOff:Number = 97;		*/				public function letters() {			// constructor code			/*			for ( var i:Number = 0; i< numLetters; i++ )			{				trace(String.fromCharCode(i+asciiOff));							}*/						circle.graphics.beginFill(0xFFCC00);			circle.graphics.drawCircle(200, 200, 200);			circle.scrollRect = new Rectangle(0, 0, 200, 200);			addChild(circle);			circle.addEventListener(MouseEvent.CLICK, clicked);		}						function clicked(event:MouseEvent):void {			//addEventListener(Event.ENTER_FRAME, animate);			var rect:Rectangle = circle.scrollRect;    		rect.y += 200;    		circle.scrollRect = rect;			circle.mouseEnabled = false;			circle.alpha = 0.12;    				}				function animate(e:Event):void		{			var rect:Rectangle = circle.scrollRect;    		rect.y -= 1;    		circle.scrollRect = rect;		}	}	}